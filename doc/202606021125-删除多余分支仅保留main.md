# 删除多余分支，仅保留 main

**时间：** 2026-06-02 11:25

---

## 提示词

用户请求：将 GitHub 上的分支删掉，只留下主分支，该项目仅一人操作，无需分支。

---

## 做了哪些事情

### 1. 检查分支状态
- 本地分支：`docs/init-setup`、`main`
- 远程分支：`origin/docs/init-setup`、`origin/main`

### 2. 删除远程分支
```bash
git push origin --delete docs/init-setup
```
- 远程分支 `docs/init-setup` 已删除

### 3. 删除本地分支
```bash
git branch -d docs/init-setup
```
- 本地分支 `docs/init-setup` 已删除（提交记录 0b30c16）

### 4. 验证结果
```bash
git branch -a
# * main
#   remotes/origin/main
```

---

## 产生结果

- 远程仓库仅剩 `main` 分支
- 本地仓库仅剩 `main` 分支
- `docs/init-setup` 分支已完全清除，不再占用仓库资源
